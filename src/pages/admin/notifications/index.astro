---
import AdminLayout from '@/layouts/AdminLayout.astro';
import WaitlistView from '@/components/admin/WaitlistView';

export const prerender = true;
---

<AdminLayout
  title="Notifications"
  subtitle="Inscrits aux notifications (liste scolaire ouverture, back-in-stock par produit). Export CSV pour campagne Brevo manuelle."
>
  <WaitlistView client:only="react" />
</AdminLayout>
