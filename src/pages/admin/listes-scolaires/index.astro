---
import AdminLayout from '@/layouts/AdminLayout.astro';
import ListesScolairesList from '@/components/admin/ListesScolairesList';

export const prerender = true;
---

<AdminLayout
  title="Listes scolaires"
  subtitle="Listes uploadées par les parents (matcher F5). Click pour voir le texte brut + matched items."
>
  <ListesScolairesList client:only="react" />
</AdminLayout>
