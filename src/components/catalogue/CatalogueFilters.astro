---
import type { SortKey } from '@/lib/queries';

export interface Props {
  categories: string[];
  brands: string[];
  selected: {
    category?: string | string[];
    brand?: string | string[];
    priceMin?: number;
    priceMax?: number;
    inStockOnly?: boolean;
    search?: string;
    sort?: SortKey;
  };
}

const { categories, brands, selected } = Astro.props;

const toArray = (v: string | string[] | undefined): string[] =>
  v == null ? [] : Array.isArray(v) ? v : [v];

const selectedCategories = new Set(toArray(selected.category));
const selectedBrands = new Set(toArray(selected.brand));

// Multi-select native is OK on desktop, mobile renders the system picker.
// 6 visible rows is enough to show the most common items without dominating
// the sidebar — overflow scrolls inside the <select>.
const SELECT_VISIBLE_ROWS = 6;
---

<form
  method="get"
  action="/catalogue"
  class="flex flex-col gap-4 rounded-card border border-primary/10 bg-white p-4"
  aria-label="Filtres catalogue"
>
  <div>
    <label for="f-search" class="label-category">Recherche</label>
    <input
      id="f-search"
      type="search"
      name="q"
      value={selected.search ?? ''}
      placeholder="Cartouche, stylo, agenda…"
      class="mt-2 h-11 w-full rounded-btn border border-primary/15 bg-white px-3 text-sm text-primary focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/30"
    />
  </div>

  <div>
    <label for="f-category" class="label-category">
      Catégories
      <span class="ml-1 text-[10px] font-normal text-primary/50">(Ctrl+clic = multi)</span>
    </label>
    <select
      id="f-category"
      name="category"
      multiple
      size={SELECT_VISIBLE_ROWS}
      class="mt-2 w-full rounded-btn border border-primary/15 bg-white px-2 py-1 text-sm text-primary focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/30"
    >
      {
        categories.map((cat) => (
          <option value={cat} selected={selectedCategories.has(cat)}>
            {cat}
          </option>
        ))
      }
    </select>
  </div>

  <div>
    <label for="f-brand" class="label-category">
      Marques
      <span class="ml-1 text-[10px] font-normal text-primary/50">(Ctrl+clic = multi)</span>
    </label>
    <select
      id="f-brand"
      name="brand"
      multiple
      size={SELECT_VISIBLE_ROWS}
      class="mt-2 w-full rounded-btn border border-primary/15 bg-white px-2 py-1 text-sm text-primary focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/30"
    >
      {
        brands.map((brand) => (
          <option value={brand} selected={selectedBrands.has(brand)}>
            {brand}
          </option>
        ))
      }
    </select>
  </div>

  <div>
    <span class="label-category">Prix TTC (€)</span>
    <div class="mt-2 grid grid-cols-2 gap-2">
      <div>
        <label for="f-price-min" class="sr-only">Prix minimum</label>
        <input
          id="f-price-min"
          type="number"
          name="priceMin"
          min="0"
          step="1"
          value={selected.priceMin ?? ''}
          placeholder="Min"
          class="h-11 w-full rounded-btn border border-primary/15 bg-white px-3 text-sm text-primary focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/30"
        />
      </div>
      <div>
        <label for="f-price-max" class="sr-only">Prix maximum</label>
        <input
          id="f-price-max"
          type="number"
          name="priceMax"
          min="0"
          step="1"
          value={selected.priceMax ?? ''}
          placeholder="Max"
          class="h-11 w-full rounded-btn border border-primary/15 bg-white px-3 text-sm text-primary focus:border-accent focus:outline-none focus:ring-2 focus:ring-accent/30"
        />
      </div>
    </div>
  </div>

  <label class="flex items-center gap-2 text-sm text-primary">
    <input
      type="checkbox"
      name="inStock"
      value="1"
      checked={selected.inStockOnly}
      class="h-4 w-4 rounded border-primary/20 text-accent focus:ring-accent"
    />
    En stock uniquement
  </label>

  <input type="hidden" name="sort" value={selected.sort ?? 'pertinence'} />

  <div class="flex items-center justify-between gap-2 pt-2">
    <a href="/catalogue" class="text-xs text-primary/60 hover:text-accent">Réinitialiser</a>
    <button
      type="submit"
      class="inline-flex h-11 items-center rounded-btn bg-primary px-4 text-sm font-medium text-white hover:bg-primary/90"
    >
      Appliquer
    </button>
  </div>
</form>
